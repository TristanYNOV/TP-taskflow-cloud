apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: staging
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
    - name: http
      port: 3002
      targetPort: http
