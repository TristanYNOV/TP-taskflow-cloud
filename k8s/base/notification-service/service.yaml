apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: staging
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 3003
      targetPort: http
